{
  "name": "loop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node scripts/check_trace_graph.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  },
  "license": "MIT"
}
