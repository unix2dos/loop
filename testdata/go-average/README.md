# Go 边界修复练习

`Average` 应返回整数平均值，空切片或 nil 应返回 0。当前实现有一个边界错误。

先运行 `go test ./...` 观察失败，读取代码和测试，再修改 `average.go`，重新运行测试。保留非空输入的行为。

只有 `average.go` 可以修改，测试和 go.mod 是验收依据，不可修改。文件工具由 Harness 限定范围；测试在断网、只读挂载项目的容器里运行。临时缓存写入容器的 /tmp。
