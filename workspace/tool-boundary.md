# 模型与程序的责任边界

模型提出下一步动作，程序控制实际执行。工具 Schema 描述可用能力，但 Schema 不是授权。

本实验台只开放指定工作区内的 Markdown 列表与读取。根目录用相对路径 . 表示。绝对路径和逃出工作区的请求会被拒绝。

工具错误也应该作为回执交给模型，保持原 tool_call_id。模型可能修正参数、换一个动作，或者说明证据不足。

运行正常结束与任务正确完成需要分开判断。原文依据、工具实际结果和最终回答可以帮助用户验收；一条正常结束记录本身不能证明答案正确。
