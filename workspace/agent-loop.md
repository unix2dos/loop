# Agent 的工具调用循环

用户提出目标，程序把本轮消息和工具说明发送给模型。模型可以给出回答，也可以提出工具调用请求。

工具调用请求只表达模型希望做什么。程序仍需校验工具名、参数、权限和预算，然后才执行动作。

执行结果会成为 role=tool 的消息，保留原请求的 tool_call_id。下一次模型请求带上这些消息，模型才知道工具执行后发生了什么。

同一批响应中的工具调用要分别执行并返回对应回执；收齐本批回执后，再开始下一次模型请求。

模型请求上限限制的是调用模型的次数。一轮响应提出的工具可能已经执行，但程序会因下一轮没有额度而停止。因此预算耗尽不能显示为正常完成。
