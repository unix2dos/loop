# 本机运行与验收

2026-09-17：历史回看已通过网页验收。服务启动后可载入五份历史记录，切换到预算耗尽案例、查看原始回执与源码；整个查看过程没有发起新模型调用。

此前服务不可用的直接证据是：8877 无监听进程，直连返回 ConnectionRefusedError。环境存在 HTTP 代理，urllib 对 127.0.0.1 的 proxy_bypass 返回 False；因此之前的超时不能等同于 Agent 循环卡住。重新启动服务后，直接 HTTP 检查返回 200。旧开发进程为何退出尚未证实，没有把重启当作永久性修复。

检查本机接口时使用直连方式，例如：

    curl --noproxy '*' http://127.0.0.1:8877/api/runs

开发服务需要保持启动终端或会话存活。重启前先确认没有活跃运行；历史查看不会恢复任务。模型服务仍沿用用户的网络配置，不全局关闭代理。

后续迁移验收中，原 Playwright 通道返回 Transport closed，而直接检查 Levon 接口仍返回 200。改用 Codex 内置浏览器后，筛选、源码关联和 JSON 导出均通过；这次异常属于验收连接中断，没有据此修改 Agent 内核。
