# 本机运行与验收

2026-09-17：历史回看已通过网页验收。服务启动后可载入五份历史记录，切换到预算耗尽案例、查看原始回执与源码；整个查看过程没有发起新模型调用。

此前服务不可用的直接证据是：8877 无监听进程，直连返回 ConnectionRefusedError。环境存在 HTTP 代理，urllib 对 127.0.0.1 的 proxy_bypass 返回 False；因此之前的超时不能等同于 Agent 循环卡住。重新启动服务后，直接 HTTP 检查返回 200。旧开发进程为何退出尚未证实，没有把重启当作永久性修复。

检查本机接口时使用直连方式，例如：

    curl --noproxy '*' http://127.0.0.1:8877/api/runs

开发服务需要保持启动终端或会话存活。重启前先确认没有活跃运行；历史查看不会恢复任务。模型服务仍沿用用户的网络配置，不全局关闭代理。

后续迁移验收中，原 Playwright 通道返回 Transport closed，而直接检查 Loop 接口仍返回 200。改用 Codex 内置浏览器后，筛选、源码关联和 JSON 导出均通过；这次异常属于验收连接中断，没有据此修改 Agent 内核。

## 模型切换验证（2026-09-17）

用户要求使用 glm-5.3-flash。当前接入渠道为 OpenCode Go API，Agent 循环、工具执行和轨迹均由 Loop 自己运行。[官方模型与接口表](https://opencode.ai/docs/go/#endpoints)及实时 models 接口均确认该模型 ID。

先在独立端口验证：运行 578376fe29df4c62a899e49f52f4f5e6 使用 glm-5.3-flash，3 次模型请求、2 次工具调用、0 次工具错误，约 5.98 秒后正常结束，并引用 workspace/agent-loop.md 中的原文。该记录已完整复制到本机历史目录，不改写以前的 MiMo 记录。这个样例证明当前工具循环可用，不作为模型质量或速度排名。

主服务使用 OPENAI_MODEL=glm-5.3-flash 启动，其他项目和全局终端环境没有修改。重启时也应显式使用这一环境变量；查看旧轨迹时，界面模型名仍以旧记录为准。

## Loop 更名与新手流程验证（2026-09-17）

项目及公开仓库统一更名为 Loop / `unix2dos/loop`，本地目录为 `<workspace>/loop`。界面标题、模块路径、客户端 User-Agent 和轨迹导出前缀均已更新；Python 参考标签和原型分支保留。移动前后，既有 27 份运行记录文件的 SHA-256 均一致；旧记录仍保留原工作区和源码。

干净检出验证了 `npm ci`、TypeScript 类型检查与构建、`go test -race ./...`、Go 二进制启动、未配置模型时的页面状态、空历史和 `.env.example` 的 shell 加载方式。四组 Python 行为对照通过。

按 README 示例在网页提交的真实运行 `66b87656adf84cb4b841d843bef805dc` 使用 glm-5.3-flash，在新工作区完成 3 次模型请求、2 次工具调用、0 次工具错误，约 6.02 秒后正常结束。工具实际列出 `agent-loop.md` 和 `tool-boundary.md`；回答的正文引文与实际文件相符，但额外写出了不存在的 `.markdown-free`。因此只确认更名后的运行链路可用，不把 `completed` 当作回答完全正确或任务验收通过，也没有重跑来替换这份记录。

## 教学关系图第一阶段验证（2026-09-17）

第一阶段将任务与回答、真实关系图、步骤说明和原始证据放在同一界面。图按模型请求展开；调用和回执通过所属批次、调用 ID、工具参数与实际消息内容核对。只有找到回执确实进入下一模型请求的证据时，才画出反馈关系。运行内核、自动执行方式及历史记录内容没有改变。

- `npm test` 的 10 项关系检查覆盖同批多个工具、错误回执、跨批与同批重复 ID、错误角色/内容、缺失 assistant 批次、截断响应、预算停止及运行中状态；TypeScript 构建通过。
- `go test -race ./...` 和四组 Python 行为对照通过；HTTP 检查覆盖关系图模块与本地记录目录元数据。
- 桌面与 390 px 窄屏验证了对话和图同页、节点关联、详情展开/收起、原始 JSON 定位、旧 Python 源码和预算停止。桌面输入区与详情区没有被内容高度挤出视口；窄屏没有横向溢出。
- 错误恢复记录中的 e009 可跳转至 e011 的 `/events/10/input/messages/5`，实际显示相同调用 ID 的 role=tool 错误回执。筛选只剩 1 条错误时，导出仍保留完整 16 条事件，与磁盘原记录逐项一致。
- 真实运行 `ac5099ce03d74a119c3457ceae1bbd0a` 使用 glm-5.3-flash，3 次模型请求、3 次工具调用、0 次工具错误。第二次响应同时提出两次 read_file；第三次请求收到两条读取回执。两处最终引文已对照实际文件核对。浏览器观察到运行中等待模型的状态，图没有预先填入尚未返回的动作。

连续任务会话和基于轨迹的自由教学问答属于已确认的第二阶段，需要在第一阶段界面验收后继续。当前说明由程序依据事件生成，不冒充模型的内部推理。

## 任务总览与紧凑轨迹迭代（2026-09-17）

- 根地址保持在所有任务总览；浏览器后退回到根地址时也不再自动进入最新任务。任务侧栏和右侧轨迹独立折叠并保存浏览器偏好。
- 全部有效历史以摘要索引列出，旧详情按需读取。新增检查用十二份历史验证超过八条的列表及最早详情可用；目录和文件软链接、非法 ID 会被拒绝，查看不改写原文件。
- 轨迹改为按模型分组的紧凑结构，点击默认进入原始记录。每步耗时、输入/输出/总 token 和缓存输入使用已有记录；缺失、不合法或部分返回的数据明确区分，不用零填充。共 13 项轨迹与用量检查通过。
- 浏览器确认了根地址不变、旧失败任务可打开、无效链接不静默切换任务、折叠刷新后保留、默认原始记录及 390 px 窄屏无横向溢出。长任务标题的内部网格溢出已修正。
- 真实验收运行 2675adf7620c4c2c9d4207f0831135fb 从“新任务”提交，上限为 1，发生 1 次模型请求和 1 次目录工具调用后以 budget_exhausted 结束，模型返回总计 470 tokens。两个侧栏保持收起，任务列表新增为 12 条；没有为回看或折叠再次调用模型。

## 分轨概览与完整事件流（2026-09-17）

本轮按用户提供的参考图改为全程分轨概览、按序事件流和可折叠工具组，取消相互割裂的摘要图与底部“全部事件”列表。记录分组不丢失、不复制、不补造事件，工具来源无法核对时明确提示。

新增检查覆盖完整事件分组守恒、预算终点及步骤/耗时概览的全量覆盖；共 16 项轨迹与用量检查通过。浏览器核对预算样例的 7 个色块与 7 条事件，以及错误恢复样例的 16 个色块与 16 条记录。点击 e004 概览会展开所在工具组，选中同一事件并显示 `/events/3`；原始记录仍为默认详情。长任务中的错误在折叠组摘要中保留提示，放大模式可恢复对话。

窄屏放大模式的指标折行已单独修正，390 px 下没有横向溢出。此轮只重构展示，未更改运行内核或已有 JSON。


## 步骤、耗时与编辑器定位验证（2026-09-17）

- 步骤条与耗时轨道同时展示，搜索输入和刻度切换已移除。预算案例各有 7 个导航标记；错误恢复案例各有 16 个标记与 16 条真实事件。点击步骤自动展开对应工具组，选中相同记录。
- 浏览器实际点击“打开本地记录”，Cursor 打开预算案例 e002 所在 run.json 的第 33 行；从错误 e009 跳到 e011 的 /input/messages/5，再点击打开，实际定位到错误恢复记录的第 511 行。两处都通过编辑器光标与文件路径核对。
- 自动检查覆盖缩进与压缩 JSON、转义字符串、嵌套数组、JSON Pointer 转义、相似 ID、缺失字段、运行中 JSONL 起止记录与不完整尾行，以及 HTTP 令牌、跨站、任意路径参数和软链接拒绝。定位不改写记录；测试不启动桌面编辑器。
- 390 px 窄屏无横向溢出，步骤与耗时可同时查看；根地址仍展示全部任务，保持原地址。此次使用已有运行验证，没有增加模型调用。


## 轨迹分栏拖动验证（2026-09-17）

桌面分界线支持左右拖动、键盘方向键调整、Home/End 到最小宽度和双击重置，比例在浏览器保存。实际拖动将轨迹从约 414 px 增加到 607 px；刷新仍保持 49% 对话比例。两侧最小宽度均为 280 px，抵达两端未产生横向溢出；折叠、放大后恢复及侧栏开合仍正确应用比例。390 px 手机布局隐藏拖动条，页面宽度保持 390 px。PROGRAM 标签增加中文悬停说明，不改变原始记录或事件分类。TypeScript 构建与 16 项轨迹检查通过；本轮没有调用模型。


## Harness 与模型轮次标签（2026-09-17）

PROGRAM 改为 HARNESS，控制轨道同步命名为 Harness；CONTEXT、RESULT、STOP 仍作为更具体的事件分类。模型行直接显示“模型轮次 N”，沿用已有分组，不增加额外轮次卡片。原始 kind、turn 和历史记录不变。浏览器核对 ac5099ce03d74a119c3457ceae1bbd0a：共三个模型轮次，第二轮的两个 read_file 同属一组，15 条原始事件全部保留。TypeScript 构建与 16 项轨迹检查通过；没有发起模型调用。


## 连续对话验证（2026-09-17）

移除“放大轨迹”；宽度仍由拖动分界线控制，轨迹栏可折叠。任务输入改为首次发送开始对话、后续直接追问；侧栏按对话合并，回答旁切换本轮轨迹。每个用户轮次保存独立 Run，通过 parent_run_id / conversation_id / conversation_turn 关联，旧记录不改写。

- 真实 GLM 验证：首轮 ec83378c687e4342b4dcdaf47318777b 告知代号“松塔-7319”；次轮 032a7c51feb44d27a9de2d27cb072d93 只询问代号，实际返回“松塔-7319”。各调用模型一次、无工具调用，合计 987 tokens。第二轮实际发送 system / user / assistant / user 四条消息，父运行和会话关联正确。
- 浏览器确认同页保留两轮消息、侧栏只新增一段对话，刷新恢复，旧回答可切回第一轮轨迹。
- Go HTTP 集成检查验证包含工具回执的额度耗尽运行可继续、每轮额度重新计数、原文件不变、服务重启后继续第三轮、旧末尾提交被拒绝、新任务无旧上下文。消息解析检查覆盖缺失/孤立工具回执、保留真实参数错误、额外 system 消息及软链接拒绝。
- Go race 检查、17 项前端关系与会话检查、4 项 Python 历史行为对照通过。原先已经独立保存的任务保持独立，不猜测关联关系。
